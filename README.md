# Motorcycle-Notification-Display
A small embedded system that displays incoming calls and selected smartphone notifications (e.g. WhatsApp) on a display mounted on a motorcycle.  The system uses an Android app to capture notifications and sends them via Bluetooth Low Energy (BLE) to an ESP32 device, which displays the information in real-time.  ## Features
